// const reportWebVitals = onPerfEntry => {
//   if (onPerfEntry && onPerfEntry instanceof Function) {
//     import('web-vitals').then(({ getCLS, getFID, getFCP, getLCP, getTTFB }) => {
//       getCLS(onPerfEntry);
//       getFID(onPerfEntry);
//       getFCP(onPerfEntry);
//       getLCP(onPerfEntry);
//       getTTFB(onPerfEntry);
//     });
//   }
// };

// export default reportWebVitals;

const reportWebVitals = (onPerfEntry) => {
  // Intentionally do nothing.
  // Keeps the same function signature as the original create-react-app helper.
  // If you later want metrics, install 'web-vitals' and restore the original implementation.
  return;
};

export default reportWebVitals;
